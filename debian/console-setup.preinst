#!/bin/sh

case "$1" in
     install|upgrade)
        if [ "$2" ]; then
            if dpkg --compare-versions "$2" lt 1.47; then
                echo Upgrading from this old version of console-setup is not supported
                exit 1
            fi
        fi
        ;;
esac
